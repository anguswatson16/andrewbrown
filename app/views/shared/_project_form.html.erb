<%= form_for(project) do |p| %>
  <%= p.label :name %>
  <%= p.text_field :name %>
  <%= p.label :description %>
  <%= p.text_field :description %>
  <%= p.label :collaborators %>
  <%= p.text_field :collaborators %>
  <%= p.label :notes %>
  <%= p.text_field :notes %>
  <%= p.submit %>
<% end %>
